# Install pipeline_core first: pip install -e ../../packages/pipeline_core

fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0
pydantic>=2.9
pydantic-settings>=2.5
httpx>=0.27
python-multipart>=0.0.9
reportlab>=4.2

# Enables live literature search (requires PAPERCLIP_API_KEY).
# The vendor wheel is not currently published on public PyPI.
gxl-paperclip @ https://paperclip.gxl.ai/paperclip.whl

pytest>=8.0
pytest-cov>=5.0
