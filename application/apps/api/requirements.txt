# Install pipeline_core first: pip install -e ../../packages/pipeline_core

fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0
pydantic>=2.9
pydantic-settings>=2.5
httpx>=0.27
python-multipart>=0.0.9
reportlab>=4.2

# The Paperclip literature SDK is NOT listed here. The vendor serves it at a URL
# whose filename pip refuses to parse ("paperclip.whl is not a valid wheel
# filename"), and one unusable line fails the whole install. Get it with:
#
#     python scripts/install_paperclip.py
#
# which downloads the same wheel and gives it the name its own metadata implies.
# Everything works without it; the literature panel just says it is unavailable.

pytest>=8.0
pytest-cov>=5.0
